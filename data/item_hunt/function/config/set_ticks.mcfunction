# Macro. Called by the timer preset buttons: set_ticks {ticks:<number>}. Redraws the menu.
$scoreboard players set ticks item_hunt_config $(ticks)
function item_hunt:config/refresh
function item_hunt:config/open
